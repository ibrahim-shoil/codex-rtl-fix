{
  "name": "codex-rtl-fix",
  "version": "0.1.0",
  "private": false,
  "description": "Mixed RTL/LTR message rendering fix for the Codex Windows app",
  "type": "module",
  "scripts": {
    "check": "node --check cdp-client.mjs && node --check rtl-payload.mjs && node --check rtl-injector.mjs && node --check rtl-apply-once.mjs && node --check rtl-diagnose.mjs",
    "test": "node test-rtl-payload.mjs"
  },
  "devDependencies": {
    "@electron/asar": "^3.4.1"
  },
  "dependencies": {
    "ws": "^8.18.3"
  }
}
